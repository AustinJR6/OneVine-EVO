import 'package:flutter/material.dart';

class GiveBackScreen extends StatelessWidget {
  const GiveBackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Give Back')),
      body: const Center(child: Text('Give Back Screen')),
    );
  }
}
